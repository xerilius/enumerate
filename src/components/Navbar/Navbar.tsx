import React from "react";

import styles from "./Navbar.module.scss";

export const Navbar = () => {
  return (
    <div className={styles.Navbar}>
      <div className={styles.Logo}>enum.</div>
    </div>
  );
};
